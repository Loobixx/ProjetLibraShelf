{% extends 'base.html.twig' %}

{% block title %}Ouvrage{% endblock %}

{% block body %}
    <h1>Ouvrage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ouvrage.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ ouvrage.titre }}</td>
            </tr>
            <tr>
                <th>Editeur</th>
                <td>{{ ouvrage.editeur }}</td>
            </tr>
            <tr>
                <th>ISBN</th>
                <td>{{ ouvrage.ISBN }}</td>
            </tr>
            <tr>
                <th>Categories</th>
                <td>{{ ouvrage.categories }}</td>
            </tr>
            <tr>
                <th>Tags</th>
                <td>{{ ouvrage.tags }}</td>
            </tr>
            <tr>
                <th>Langues</th>
                <td>{{ ouvrage.langues }}</td>
            </tr>
            <tr>
                <th>Année</th>
                <td>{{ ouvrage.annee }}</td>
            </tr>
            <tr>
                <th>Résumé</th>
                <td>{{ ouvrage.résumé }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ouvrage_index') }}">back to list</a>

    <a href="{{ path('app_ouvrage_edit', {'id': ouvrage.id}) }}">edit</a>

    {{ include('ouvrage/_delete_form.html.twig') }}
{% endblock %}
